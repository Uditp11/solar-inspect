# Environment for solar-inspect. Python 3.11 on Windows 11, CUDA 12.8.
#
#     python -m venv .venv && .venv\Scripts\activate
#     pip install -r requirements.txt
#
# torch and torchvision are pinned to +cu128 local versions, which exist only on
# PyTorch's own index -- so --extra-index-url below is what makes this file
# executable rather than descriptive. Without it the resolver falls back to
# PyPI's default wheel, which is CPU-only, and every GPU run in this repo then
# silently runs on the CPU instead of failing. Re-check after any pip install:
#
#     python -c "import torch;print(torch.__version__, torch.version.cuda, torch.cuda.is_available())"
#     -> 2.11.0+cu128 12.8 True
#
# --extra-index-url rather than --index-url: the cu128 index hosts only the
# torch packages, so making it the sole index would break the other 79 lines.
# The +cu128 local version identifier is absent from PyPI, so there is no
# ambiguity about which index can satisfy torch.

--extra-index-url https://download.pytorch.org/whl/cu128

anyio==4.14.2
attrs==26.1.0
bleach==6.4.0
certifi==2026.7.22
charset-normalizer==3.5.1
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
fastjsonschema==2.22.2
filelock==3.32.3
fonttools==4.63.0
fsspec==2026.7.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
Jinja2==3.1.6
joblib==1.5.3
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
jupyter_core==5.9.1
jupytext==1.19.5
kaggle==2.2.4
kagglesdk==0.1.37
kiwisolver==1.5.0
lightning-utilities==0.15.3
markdown-it-py==4.2.0
MarkupSafe==3.0.3
matplotlib==3.11.1
mdit-py-plugins==0.6.1
mdurl==0.1.2
motmetrics==1.4.0
mpmath==1.3.0
narwhals==2.25.0
nbformat==5.11.1
networkx==3.6.1
numpy==2.4.6
nvidia-ml-py==13.610.43
opencv-python==5.0.0.93
packaging==26.3
pandas==3.0.5
patsy==1.0.2
pillow==12.3.0
platformdirs==4.11.4
pluggy==1.6.0
polars==1.44.1
polars-runtime-32==1.44.1
protobuf==7.36.0
psutil==7.2.2
pycocotools==2.0.11
Pygments==2.21.0
pyparsing==3.3.2
pytest==9.1.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
python-slugify==8.0.4
PyYAML==6.0.3
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
scikit-learn==1.9.0
scipy==1.17.1
six==1.17.0
statsmodels==0.14.6
sympy==1.14.0
text-unidecode==1.3
threadpoolctl==3.6.0
torch==2.11.0+cu128
torchmetrics==1.9.0
torchvision==0.26.0+cu128
tqdm==4.70.0
traitlets==5.16.1
typing_extensions==4.16.0
tzdata==2026.3
ultralytics==8.4.129
ultralytics-platform==0.1.16
ultralytics-thop==2.1.6
urllib3==2.7.0
webencodings==0.6.1
xmltodict==1.0.4
